import {css} from 'lit';
import { common } from '../../../common/styles/common.style';

export const style = [common, css`
    .ing-logo {
        display: block;
    }

    .ing-logo img {
        width: auto;
        height: 32px;
        margin: 24px 0;
    }
`];